"""The resident menu-bar app (macOS): hold a key anywhere, get a compiled
session bundle and a notification. Controller logic is platform-free and
testable; the rumps shell and LaunchAgent installer are macOS-only."""
